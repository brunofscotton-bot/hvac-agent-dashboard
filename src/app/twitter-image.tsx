// Twitter uses the same image as OpenGraph for now.
// Re-export so Next.js picks it up automatically.
export { default } from "./opengraph-image";
export { runtime, size, contentType } from "./opengraph-image";
export const alt = "Ringa: Your front desk, always on";
